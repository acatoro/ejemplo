name: ejemplo

on: 
 push:
 workflow_dispatch:

jobs:
  ejemplo:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/setup-node@v3.7.0
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: cazziwork/create-release-note@v1.0.0
      with:
       github-token: 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
       tag: 1.0
